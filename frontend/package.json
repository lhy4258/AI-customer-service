{
  "name": "project1-customer-service-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode customer --host 127.0.0.1 --port 5173 --strictPort",
    "dev:customer": "vite --mode customer --host 127.0.0.1 --port 5173 --strictPort",
    "dev:support": "vite --mode support --host 127.0.0.1 --port 5174 --strictPort",
    "build": "vite build --mode customer",
    "build:customer": "vite build --mode customer --outDir dist/customer",
    "build:support": "vite build --mode support --outDir dist/support",
    "preview": "vite preview --host 127.0.0.1 --port 4173 --strictPort"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.0.7",
    "vue": "^3.5.13"
  },
  "devDependencies": {}
}
